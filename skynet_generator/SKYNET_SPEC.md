# Skynet Generator Spec

See `movie_generator.py`, `refinement.py`, and `universe_bible.yaml`. This first pass ports the HallTime architecture into a sci-fi villain incident generator: same feedback loop, new canon, reset corpus.
